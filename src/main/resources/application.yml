
server:
  port: 8083

spring:
  application:
    name: ai-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/trackfit_fitness_rec_ai_db
      database: trackfit_fitness_rec_ai_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/